# Bina Language Semantics

## Стратегия вычисления

Язык Bina использует **call-by-value** — все аргументы передаются в функцию
по значению. При вызове функции каждый аргумент копируется в локальную
переменную-параметр. Изменение параметра внутри функции не влияет на значение
в вызывающем контексте.

```bina
fn increment(x: int32) -> int32 {
    return x + 1;
    // x — локальная копия, оригинал не затронут
}
```

---

## Семантика присваивания

Все присваивания выполняют копирование по значению (value semantics).

```bina
let a: [3]int32 = [1, 2, 3];
mut b: [3]int32 = a;   // b — независимая копия
b[0] = 999;            // a[0] остаётся равным 1
```

Это относится ко всем типам: числам, строкам, массивам и структурам.

---

## Инициализация переменных

Каждая переменная должна быть инициализирована в момент объявления.
Использование неинициализированной переменной — ошибка компиляции.

```bina
let x: int32 = 0;      // ок
let y: int32;           // ошибка компиляции: отсутствует инициализатор
```

---

## Мутабельность

Переменные объявленные через `let` — иммутабельные (неизменяемые).
Переменные объявленные через `mut` — мутабельные (изменяемые).

```bina
let x: int32 = 10;
x = 20;                // ошибка компиляции: x иммутабельна

mut y: int32 = 10;
y = 20;                // ок
```

Поля структур наследуют мутабельность переменной, в которой хранится структура:

```bina
mut p: Point = Point { x: 1.0, y: 2.0 };
p.x = 3.0;             // p мутабельна

let q: Point = Point { x: 1.0, y: 2.0 };
q.x = 3.0;             // ошибка: q иммутабельна
```

---

## Время жизни переменных

Переменная существует в пределах блока `{ ... }`, в котором объявлена.
По выходу из блока переменная уничтожается.

```bina
fn example() -> void {
    let x: int32 = 10;     // x существует здесь
    if true {
        let y: int32 = 20; // y существует только внутри if
    }
    // y больше не существует
    // x всё ещё существует
}
// x больше не существует
```

---

## Управление памятью

Язык использует value semantics и не предоставляет ручного управления памятью.
В Bina нет `free`, `delete`, указателей, ссылок и операций явного освобождения.

Строки являются immutable-значениями. Данные строк размещаются в runtime
heap/arena, а конкатенация строк создаёт новую строку.

Массивы имеют фиксированный размер, являются значениями и копируются при
присваивании, инициализации и передаче в функцию. Структуры ведут себя так же:
значение структуры копируется целиком.

Базовая модель учебного runtime: память, выделенная под строки и другие
runtime-объекты, может освобождаться целиком при завершении программы.
Отдельного освобождения объектов во время выполнения не требуется.

---

## Область видимости и разрешение имён

### Лексическая область видимости

Идентификатор разрешается в ближайшей охватывающей области видимости,
начиная с текущего блока и поднимаясь вверх. Для переменных поиск ограничивается
телом функции (глобальных переменных в языке нет), а для функций и типов —
поднимается до пространств имён и глобального уровня.

### Теневое перекрытие (Shadowing)

Внутреннее объявление имени скрывает внешнее в пределах своего блока:

```bina
let x: int32 = 10;
if true {
    let x: int32 = 20;  // новый x, скрывает внешний
    print(x);            // выводит 20
}
print(x);                // выводит 10 (внешний x не изменён)
```

### Запрет использования до объявления

Локальная переменная не может быть использована до её объявления в текущей
области видимости:

```bina
print(x);               // ошибка: x ещё не объявлен
let x: int32 = 42;
```

Это правило относится **только к локальным переменным**.

### Видимость объявлений верхнего уровня

Функции, структуры, синонимы типов и пространства имён видны во всей
охватывающей области видимости **независимо от порядка их объявления**.
Их можно использовать до текстового места объявления — в частности, функция
может вызывать другую функцию, объявленную ниже в файле:

```bina
fn main() -> int {
    return helper();    // ок: helper объявлена ниже
}

fn helper() -> int {
    return 42;
}
```

То же справедливо для взаимной рекурсии, ссылок на структуру в сигнатурах
функций, объявленных раньше структуры, и т.п. Компилятор сначала собирает
все объявления верхнего уровня области, и лишь затем анализирует тела функций.

### Запрет повторного объявления

Одно и то же имя не может быть объявлено дважды в одной области видимости:

```bina
let x: int32 = 1;
let x: int32 = 2;       // ошибка: x уже объявлен в этой области
```

---

## Пространства имён

Пространство имён группирует объявления под общим именем.
Доступ к элементам — через оператор `::`.

```bina
namespace Math {
    fn abs(x: int32) -> int32 {
        if x < 0 {
            return 0 - x;
        }
        return x;
    }
}

fn main() -> int {
    let result = Math::abs(0 - 5);
    print(result);       // 5
    return 0;
}
```

Пространства имён могут быть вложенными:

```bina
namespace A {
    namespace B {
        fn f() -> void { print("A::B::f"); }
    }
}

fn main() -> int {
    A::B::f();
    return 0;
}
```

---

## Методы структур (impl)

Блок `impl` привязывает функции (методы) к конкретной структуре.

### Правила

1. Первый параметр метода должен называться `self` и иметь тип
   соответствующей структуры. Параметр `self` передаётся по значению (копия).
2. При вызове метода через точку (`obj.method(args)`) объект автоматически
   подставляется как первый аргумент (`self`).
3. Один блок `impl` на одну структуру. Повторный `impl` для той же структуры —
   ошибка компиляции.
4. Блок `impl` должен располагаться в той же области видимости,
   что и объявление структуры.

### Пример

```bina
struct Circle {
    pub radius: float64;
}

impl Circle {
    pub fn area(self: Circle) -> float64 {
        return 3.14159 * self.radius * self.radius;
    }

    pub fn scale(self: Circle, factor: float64) -> Circle {
        return Circle { radius: self.radius * factor };
    }
}

fn main() -> int {
    let c = Circle { radius: 5.0 };
    let a = c.area();           // эквивалентно Circle::area(c)
    print(a);

    let big = c.scale(2.0);    // эквивалентно Circle::scale(c, 2.0)
    print(big.area());
    return 0;
}
```

### Вызов как обычной функции

Методы также можно вызвать как обычные функции через квалифицированное имя:

```bina
let c = Circle { radius: 5.0 };
let a = Circle::area(c);       // явный вызов, эквивалентно c.area()
```

---

## Видимость членов структуры

Поля структуры и методы в `impl` могут быть публичными или приватными.

### Правила

1. Модификатор `pub` делает поле или метод публичным.
2. Поля и методы без `pub` являются приватными.
3. Приватные поля доступны только внутри методов той же структуры.
4. Приватные методы можно вызывать только из методов той же структуры.
5. «Та же структура» определяется по типу, а не по конкретному экземпляру:
   метод структуры может читать приватные поля любого значения этого же
   структурного типа.

Проверка приватности выполняется в следующих точках:

- чтение поля: `obj.field`;
- запись поля: `obj.field = value`;
- инициализация поля в литерале структуры: `Struct { field: value }`;
- вызов метода через точку: `obj.method()`;
- квалифицированный вызов метода: `Struct::method(obj)`.

Операции над значением структуры целиком не считаются доступом к имени
конкретного поля. Поэтому:

- `print(value)` печатает все поля структуры, включая приватные;
- `==` и `!=` сравнивают все поля структуры, включая приватные.

Структура с приватными полями не может быть создана литералом снаружи методов
этой же структуры: приватное поле нельзя инициализировать, а пропуск поля
остаётся ошибкой отсутствующего инициализатора. В текущей версии языка нет
статических методов или конструкторов без `self`, поэтому для внешнего создания
значений через литерал поля, которые нужно задавать снаружи, должны быть `pub`.

### Пример

```bina
struct Counter {
    pub value: int32;
}

impl Counter {
    pub fn next(self: Counter) -> int32 {
        return self.private_next();
    }

    fn private_next(self: Counter) -> int32 {
        return self.value + 1;
    }
}

fn main() -> int {
    let c = Counter { value: 41 };
    print(c.value);       // ok: поле публичное
    print(c.next());      // ok: метод публичный
    c.private_next();     // ошибка: метод приватный
    return 0;
}
```

---

## Семантика выражений

### Арифметические операции

| Оператор | Операнды | Результат |
|----------|----------|-----------|
| `+` | два числа | число общего расширенного типа |
| `+` | две строки | конкатенация (новая строка) |
| `-` | два числа | число общего расширенного типа |
| `*` | два числа | число общего расширенного типа |
| `/` | два числа | число общего расширенного типа |
| `%` | два целых числа | целое число общего расширенного типа |

Деление целых чисел — целочисленное (отбрасывание дробной части).
Целочисленное деление и остаток от деления на ноль — ошибка во время
выполнения. Переполнение при `INT_MIN / -1` и `INT_MIN % -1` для знаковых
типов — ошибка `integer overflow`. Вещественное деление на ноль не является
ошибкой: результат — `inf`, `-inf` или `nan` по IEEE 754.

Если типы операндов различаются, применяются правила неявного приведения
(см. `types.md`, раздел «Неявное приведение типов»). Результат будет иметь 
общий расширенный тип.

### Операции сравнения

| Оператор | Операнды | Результат |
|----------|----------|-----------|
| `==`, `!=` | два значения | `bool` |
| `<`, `>`, `<=`, `>=` | два числа | `bool` |

Сравнение `==` и `!=` допускается для чисел, `bool`, `char`, строк,
массивов одного типа и структур одного номинального типа. Для чисел
применяются правила общего расширенного типа.

Строки при `==` и `!=` сравниваются побайтово как UTF-8.
Сравнение `<`, `>`, `<=`, `>=` для строк **не допускается**.

Массивы сравниваются поэлементно. Массивы можно сравнить только если они имеют
один и тот же тип: совпадают размер и тип элемента. Тип элемента тоже должен
поддерживать `==`/`!=`.

Структуры сравниваются по полям. Структуры можно сравнить только если это один
и тот же номинальный тип структуры. Все поля структуры тоже должны
поддерживать `==`/`!=`.

Сравнение `<`, `>`, `<=`, `>=` для `char`, массивов и структур **не
допускается**.

Цепочки сравнений (`a < b < c`) запрещены — сравнение не ассоциативно.

### Логические операции

| Оператор | Описание | Вычисление |
|----------|----------|------------|
| `&&` | логическое И | ленивое (short-circuit) |
| `\|\|` | логическое ИЛИ | ленивое (short-circuit) |
| `!` | логическое НЕ | — |

Операнды должны иметь тип `bool`. При ленивом вычислении:
- `a && b` — если `a` равно `false`, `b` не вычисляется.
- `a || b` — если `a` равно `true`, `b` не вычисляется.

### Унарные операции

| Оператор | Операнд | Результат |
|----------|---------|-----------|
| `-` (унарный минус) | число | число того же типа |
| `!` (отрицание) | `bool` | `bool` |

### Явные приведения

`cast<T>(expr)` вычисляет `expr`, затем явно преобразует полученное значение к
типу `T` по правилам из `types.md`.

При `cast<string>(number)` создаётся новая строка с текстовым представлением
числа. При `cast<int*>(string)` и `cast<uint*>(string)` строка разбирается как
целое число целевого типа. Строка должна точно соответствовать формату
числового литерала с необязательным ведущим знаком `-`; пробелы, знак `+` и
шестнадцатеричная запись недопустимы. Если строка не является корректным
числом, возникает `runtime error: invalid numeric conversion at line <N>`.
Если значение не помещается в целевой целочисленный тип, возникает
`runtime error: integer overflow at line <N>`.

При `cast<float*>(string)` строка разбирается как вещественное число.
Специальные строки `"inf"`, `"-inf"`, `"nan"` и `"NaN"` допустимы.

При `cast<int*>(float*)` и `cast<uint*>(float*)` дробная часть отбрасывается.
Если результат не помещается в целевой тип, а также для значений `NaN` и
`±inf`, возникает `runtime error: integer overflow at line <N>`.

### Индексирование массива

```bina
let arr: [5]int32 = [10, 20, 30, 40, 50];
let x = arr[2];         // x = 30
```

Индекс должен быть целочисленного типа. Индексация с нуля.
Выход за границы массива — ошибка во время выполнения.

### Доступ к полю структуры

```bina
let p = Point { x: 1.0, y: 2.0 };
let a = p.x;            // a = 1.0
```

Обращение к несуществующему полю — ошибка компиляции.

---

## Семантика инструкций

### Ветвление (if / else)

Условие должно иметь тип `bool`. Допускается цепочка `else if`:

```bina
if x > 0 {
    print("positive");
} else if x == 0 {
    print("zero");
} else {
    print("negative");
}
```

### Цикл (while)

Условие должно иметь тип `bool`. Тело выполняется, пока условие истинно.

```bina
mut i: int32 = 0;
while i < 10 {
    print(i);
    i = i + 1;
}
```

### break / continue

`break` — немедленный выход из ближайшего цикла `while`.
`continue` — переход к следующей итерации ближайшего цикла `while`.

Использование `break` или `continue` вне тела цикла — ошибка компиляции.

### return

Возврат значения из функции. Тип возвращаемого значения должен совпадать
с типом возврата функции (с учётом неявного расширения).

В функциях с типом возврата `void` — `return;` без значения.

Если управление достигает конца тела функции с не-`void` типом возврата
без выполнения `return` — ошибка компиляции.

---

## Семантика функций

### Объявление

Функция объявляется на верхнем уровне программы, внутри пространства имён
или внутри блока `impl`. Вложенные функции (функция внутри функции) запрещены.

### Точка входа

Программа обязана содержать функцию `main` с типом возврата `int`:

```bina
fn main() -> int {
    return 0;
}
```

Возвращаемое значение `main` передаётся как код завершения процесса.

### Порядок вычисления аргументов

Аргументы функции вычисляются слева направо перед вызовом.

---

## Перегрузка функций

Язык Bina поддерживает **перегрузку функций** — несколько функций с одним
именем, различающихся сигнатурой (числом или типами параметров).

### Правила объявления

1. Допустимо объявлять несколько функций с одним именем, если их сигнатуры
   различаются хотя бы одним параметром (количеством или типом).
2. Перегрузки **не могут** различаться только типом возврата — выбор
   перегрузки производится по аргументам вызова.
3. Перегрузки видят друг друга только в пределах одной области видимости
   (глобальный уровень, конкретное пространство имён или блок `impl`).
   Функции с одинаковым именем в разных областях — независимы.

```bina
fn area(r: float64) -> float64 {
    return 3.14159 * r * r;
}

fn area(w: float64, h: float64) -> float64 {
    return w * h;
}

fn area(s: int32) -> int32 {           // другой тип параметра — ок
    return s * s;
}

fn area() -> float64 { return 0.0; }   // ок: нет параметров

fn area(r: float64) -> int32 { ... }   // ошибка: отличается только тип возврата
```

### Разрешение вызова

При вызове функции компилятор выполняет следующий алгоритм:

1. Собирает все функции с заданным именем в текущей области видимости.
2. Отбрасывает те, у которых число параметров не совпадает с числом аргументов.
3. Для каждой оставшейся функции вычисляет качество совпадения каждого
   аргумента с соответствующим параметром:
   - **EXACT** — тип аргумента точно совпадает с типом параметра;
   - **PROMOTION** — расширение внутри одного семейства типов:
     `int8 → int16 → int32 → int64`,
     `uint8 → uint16 → uint32 → uint64`,
     `float32 → float64`;
   - **CONVERSION** — остальные допустимые неявные расширения из графа
     `types.md`, например `uint32 → int64` или `int32 → float64`;
   - если аргумент нельзя неявно привести к параметру, кандидат отбрасывается.
4. Качество кандидата — худшее качество среди всех его аргументов:
   `EXACT` лучше `PROMOTION`, `PROMOTION` лучше `CONVERSION`.
5. Выбираются кандидаты с лучшим качеством.
6. Если таких кандидатов несколько, применяется tie-break: суммарная длина
   кратчайших путей неявного расширения по графу из `types.md`. Меньшая сумма
   означает более конкретную перегрузку.
7. Результат:
   - ровно один лучший кандидат → вызов разрешён;
   - кандидатов нет → ошибка `no matching overload for <name>`;
   - после всех правил осталось несколько лучших кандидатов → ошибка
     `ambiguous call to <name>`.

```bina
fn f(x: int32) -> int32   { return 1; }
fn f(x: float64) -> int32 { return 2; }

f(10);       // вызывает f(int32) — точное совпадение
f(3.14);     // вызывает f(float64) — точное совпадение
f(10i8);     // вызывает f(int32) — расширение int8 → int32
```

Пример выбора более конкретного типа:

```bina
fn g(x: int16) -> int32 { return 1; }
fn g(x: int64) -> int32 { return 2; }

g(1i8);  // вызывает g(int16): оба кандидата PROMOTION,
         // но int8 → int16 короче, чем int8 → int64
```

Пример предпочтения promotion над conversion:

```bina
fn h(x: uint64) -> int32 { return 1; }
fn h(x: int64) -> int32  { return 2; }

h(1u32); // вызывает h(uint64): uint32 → uint64 является PROMOTION,
         // а uint32 → int64 является CONVERSION
```

Пример неоднозначности:

```bina
fn g(x: int64, y: int32) -> int32 { ... }
fn g(x: int32, y: int64) -> int32 { ... }

g(1i32, 1i32);  // ошибка: одинаковое качество и одинаковая сумма расстояний
```

### Перегрузка методов

Методы в блоке `impl` перегружаются по тем же правилам. Перегрузки видят
друг друга только внутри одного блока `impl`.

```bina
impl Vec {
    fn scale(self: Vec, k: float64) -> Vec { ... }
    fn scale(self: Vec, kx: float64, ky: float64) -> Vec { ... }
}
```

## Перегрузка операторов

Пользовательские типы-структуры могут задавать реализацию встроенных
операторов через специальные методы в своём `impl`.

Диспетчеризация выполняется по типу левого операнда. Если левый операнд имеет
тип структуры `T`, компилятор ищет соответствующий метод `T::op_*`; перегрузки
по типу правого операнда выбираются обычными правилами разрешения перегрузок.
Запись `1 + vec` не вызывает метод `vec`, потому что левый операнд не является
структурой.

| Оператор | Метод | Сигнатура | Возврат |
|----------|-------|-----------|---------|
| `a + b` | `op_add` | `(self: T, other: U)` | любой не-`void` |
| `a - b` | `op_sub` | `(self: T, other: U)` | любой не-`void` |
| `a * b` | `op_mul` | `(self: T, other: U)` | любой не-`void` |
| `a / b` | `op_div` | `(self: T, other: U)` | любой не-`void` |
| `a % b` | `op_rem` | `(self: T, other: U)` | любой не-`void` |
| `a == b`, `a != b` | `op_eq` | `(self: T, other: T)` | строго `bool` |
| `a < b` | `op_lt` | `(self: T, other: U)` | строго `bool` |
| `a <= b` | `op_le` | `(self: T, other: U)` | строго `bool` |
| `a > b` | `op_gt` | `(self: T, other: U)` | строго `bool` |
| `a >= b` | `op_ge` | `(self: T, other: U)` | строго `bool` |
| `-a` | `op_neg` | `(self: T)` | любой не-`void` |

`!=` не имеет отдельного метода: для структур это отрицание результата
`op_eq`.

`&&`, `||` и `!` не перегружаются, потому что для них важна встроенная
логическая семантика (`&&` и `||` вычисляются с short-circuit).

Если у структуры нет `op_eq`, `==` и `!=` используют встроенное сравнение:
структуры сравниваются по полям, массивы — по элементам. Если у структуры есть
корректный `op_eq(self: T, other: T) -> bool`, он полностью заменяет
пополевое сравнение этого типа, в том числе при сравнении массивов таких
структур и структур, содержащих этот тип полем.

Операторные методы подчиняются правилам видимости членов структуры. Приватный
`op_*` доступен только из методов той же структуры; вне её `impl` такой
оператор считается недоступным.

Прямой вызов operator-method разрешён как обычный вызов метода:

```bina
let c = a + b;
let d = a.op_add(b);  // эквивалентно, если op_add публичен и подходит по типам
```

### Встроенные функции

Встроенные функции (`print`, `input`, `len`, `assert`, `code`, `char_from`,
`columbina`, `exit`, `panic`)
**не подлежат перегрузке**. Объявление пользовательской функции с одним из
этих имён — ошибка компиляции.

---

## Встроенные функции

| Функция | Аргументы | Возврат | Описание |
|---------|-----------|---------|----------|
| `print(expr)` | любой тип | `void` | Вывод значения в stdout. Числа и строки выводятся в текстовом виде, `bool` — как `true`/`false`. Перевод строки не добавляется автоматически. |
| `input()` | нет | `string` | Чтение строки из stdin (до символа новой строки). |
| `columbina()` | нет | `void` | Выводит встроенный ASCII-art в stdout. |
| `len(expr)` | `string` или массив | `int64` | Длина строки (в байтах) или количество элементов массива. |
| `assert(expr)` | `bool` | `void` | Проверяет условие. Если значение `false`, завершает программу с runtime error. |
| `code(expr)` | `char` | `int64` | Возвращает Unicode-код символа. |
| `char_from(expr)` | целое число | `char` | Возвращает символ по Unicode-коду. |
| `exit(expr)` | целое число | не возвращает | Немедленное завершение программы с указанным кодом возврата. |
| `panic(expr)` | `string` | не возвращает | Аварийное завершение программы. Выводит сообщение в stderr и завершает процесс с кодом 1. |

---

## Строковые операции

Строки в Bina — неизменяемые (immutable) последовательности байтов в UTF-8.

| Операция | Синтаксис | Описание |
|----------|-----------|----------|
| Конкатенация | `s1 + s2` | Создаёт новую строку |
| Длина | `len(s)` | Количество байтов |
| Сравнение | `s1 == s2`, `s1 != s2` | Побайтовое сравнение |

```bina
let greeting = "hello" + " " + "world";
let length = len(greeting);    // 11
```

---

## Ошибки во время выполнения

При возникновении ошибки во время выполнения программа завершается
с сообщением в stderr в формате:

```
runtime error: <сообщение> at line <N>
```

Обязательно обрабатываемые ошибки:

| Ошибка | Сообщение |
|--------|-----------|
| Деление на ноль | `division by zero` |
| Выход за границы массива | `index out of bounds` |
| Целочисленное переполнение | `integer overflow` |
| Проваленная проверка `assert` | `assertion failed` |
| Некорректная строка при приведении к числу | `invalid numeric conversion` |
| Недопустимый Unicode-код для `char_from` | `invalid character code` |
