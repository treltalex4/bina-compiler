struct Point {
    x: float64;
    y: float64;
}

impl Point {
    fn length(self: Point) -> float64 {
        return self.x + self.y;
    }
}

fn main() -> int {
    let p = Point { x: 2.0, y: 3.0 };
    let a = p.length();
    let b = Point::length(p);
    print(a);
    print(b);
    return 0;
}
